# ListDirectory
Just a basic Python and EXE to list a directory and spit out a tree.txt list.

Packed with PyInstaller to make the exe.
`python -m PyInstaller --onefile --name=list --icon=tree.ico list.py`
